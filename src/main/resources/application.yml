server:
  port: 8888
  error:
    include-message: always
    include-binding-errors: always

# JWT Configuration
jwt:
  secret: your-256-bit-secret-key-change-this-in-production-make-it-long-and-secure
  expiration: 86400000
# 86400000 ms = 24 hours

spring:
  security:
    user:
      name: aamir
      password: abc123
  datasource:
    url: jdbc:h2:file:./data/gopnikDB
    driver-class-name: org.h2.Driver
    username: sa
    password: password

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: aamirkamaal1994@gmail.com
    password: xxxx xxxx xxxx xxxx
    properties:
      mail:
        smtp:
          ssl:
            trust: "*"
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000


